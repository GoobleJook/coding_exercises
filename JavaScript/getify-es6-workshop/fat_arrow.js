// Kyle is not a fan of arrow functions, because:
// 1. Can be variation in syntax, unlike the standard function

// foo = () => 2;

// same as 

// function foo () {
// 	return 2;
// }

// some variations:
// () => 3 -- these indicate parameters not function
// x => 3
// (...x) => 3
// (x,y) => 3

// Concise function body (all legal expressions allowed; statements are not expressions)

// x => { try { 3; } catch(e) {} }

// Arrow functions are syntacticly anonymous, so no named functions