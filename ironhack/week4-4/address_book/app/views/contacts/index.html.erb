<ul>
	<% @contacts.each do |contact| %>
	<li>
		<a href="/contacts/<%= contact.id %>" >
		<%= contact.name %></a>
	</li>
		
	<% end %>
</ul>

<a href="/contacts/new">
	<button>Create a new contact!</button>
</a>